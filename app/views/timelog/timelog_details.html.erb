
	<% if @issues.empty? %>
		<p class="nodata"><%= l(:label_no_data) %></p>
	<% else %>
		<%#= render_query_totals(@query) %>
		<%= render :partial => 'timelog/list_timelog_details', :locals => {:issues => @issues} %>

	<% end %>